# Backup antes da OpenEmuARM 2.1.3 — PokeMini

Data: 19 de setembro de 2026

Este backup registra o estado do projeto antes de reativarmos o core PokeMini para a versão 2.1.3.

## Conteúdo

- `OpenEmuARM/` — cópia do código-fonte e dos arquivos de trabalho existentes no momento do backup.
- Tamanho da cópia: aproximadamente 2,9 GB.
- Branch no momento do backup: `fix/update-v2.1.2-appcast-final`.
- Commit HEAD no momento do backup: `173d33334`.

## Exclusões intencionais

Foram excluídos somente dados reconstruíveis ou cópias que já estavam dentro do repositório:

- `.git/`
- `Backups/`
- `.build-validation/`
- diretórios `build/` e DerivedData dos cores
- DMG em `Releases/`
- estado de interface do usuário do Xcode (`*.xcuserstate`)

O backup não altera nem remove o projeto original.
